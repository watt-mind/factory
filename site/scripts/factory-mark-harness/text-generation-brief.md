Design four materially different symbol-only SVG logo candidates for Factory,
an autonomous software-delivery runtime by Watt Mind. Factory coordinates
bounded agent execution, independent verification, feedback, and durable
evidence. The current mark is a slightly tangled infinity loop; the user wants
something more deliberate and is open to abandoning that silhouette.

Return the SVG source as structured data only. You have no tools and must not
attempt to read or write files.

Create these four directions:

1. `01-precision-loop.svg` — a highly legible continuous-loop direction with
   one unambiguous weave and much less visual noise than a typical infinity
   logo.
2. `02-control-plane.svg` — a non-infinity direction expressing a deterministic
   control plane admitting and routing bounded work.
3. `03-recursive-gate.svg` — a non-infinity direction combining recursion with
   a visible verification gate or acceptance boundary.
4. `04-bounded-flow.svg` — a non-infinity direction where parallel execution
   paths converge into one accepted durable result.

Hard requirements for every SVG:

- Transparent canvas and exact `viewBox="0 0 640 420"`.
- No background rectangle, raster image, external resource, script, text,
  wordmark, gradient, glow, shadow, blur, or texture.
- Flat geometry only: paths, lines, polygons, circles, masks, and clip paths.
- Dark-background presentation using only light ink `#dde4e1`, primary
  `#4fdbc8`, primary-container `#14b8a6`, and surface `#0e1513` when a masking
  stroke is required.
- Strong at 48px and distinctive at 200–320px.
- Prefer 10–18px main strokes and a consistent cap/join language.
- At most three teal nodes or accents, each semantically meaningful.
- At most one clear over/under relationship.
- Avoid tiny circuit traces, excessive arrowheads, recycling symbols, generic
  DevOps clip-art, arbitrary hexagons, and decorative symmetry.
- Use a 4px coordinate grid wherever practical and leave comfortable margins.
- Include concise `<title>` and `<desc>` elements with unique IDs.
- Emit valid standalone XML SVG source. Self-review path syntax, clipping,
  balance, and 48px survival before responding.

The four silhouettes must be unmistakably different from one another. Prefer a
small number of confident geometric decisions over intricate illustration.
