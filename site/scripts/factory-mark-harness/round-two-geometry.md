Continue refining the Factory `Recursive Gate` direction. The concept is
approved, but the current final SVG still feels like a system diagram rather
than a resolved brand mark.

Problems visible in the rendered mark:

- The tall teal post dominates and makes the form resemble a safe or vault.
- The open nested frames still carry a letter-C association.
- The core, stem, gate, and exit line read as four diagram components instead
  of one authored silhouette.
- Horizontal/vertical weights are technically equal but not optically balanced.
- At hero size, too much of the meaning depends on explaining the diagram.

Create three materially different refinements while preserving the approved
idea: bounded work inside nested execution envelopes reaches a deterministic
verification boundary and exits as accepted evidence.

Return exactly these directions:

1. `06-integrated-gate.svg` — the verification gate is structurally integrated
   into both envelopes. No freestanding full-height post. Compact, calm,
   monolithic silhouette.
2. `07-verification-seam.svg` — the gate becomes a short teal seam or aperture
   in the outer boundary. The accepted exit should feel like the natural
   continuation of the enclosed core, not an attached flowchart line.
3. `08-recursive-aperture.svg` — reinterpret the nested envelopes through
   repeated corners, folded paths, or apertures rather than two rounded C
   shapes. Most abstract and brand-like of the three, but still legible.

Hard requirements:

- Return structured data only; do not use tools or read/write files.
- Exact transparent `viewBox="0 0 640 420"`; no background rectangle.
- Dark-background presentation using only `#dde4e1`, `#4fdbc8`, `#14b8a6`,
  and `#0e1513` only for masking when essential.
- No text, wordmark, gradient, shadow, glow, blur, texture, arrows, tiny circuit
  details, or generic hexagon/shield/recycling motifs.
- Flat SVG geometry, valid XML, unique title/desc IDs.
- One confident silhouette, at most two teal accents, at most one overlap.
- Main strokes 14–20px with one consistent cap/join language.
- Center the optical mass, use comfortable margins, and design for 32px,
  48px, and 300px simultaneously.
- The core and exit must belong to the silhouette rather than looking overlaid.
- Avoid literal letterforms, but a subtle structural echo of `F` is acceptable
  only if it emerges naturally and does not read as a monogram.
- Self-review every path, aperture, and negative-space width before responding.

For each candidate, return filename, name, one concise rationale, and complete
standalone SVG source.
