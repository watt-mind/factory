Refine your `03-recursive-gate.svg` direction into one final candidate. It is the
strongest concept because the nested bounded execution and deterministic
acceptance boundary are specific to Factory.

Focused critique from the rendered comparison:

- The visual mass sits too far left and leaves excessive dead space on the
  right. Rebalance and center the complete silhouette within 640×420.
- The two open rounded frames currently read a little like a generic letter C.
  Make the boundaries feel like nested execution envelopes or gates instead of
  typography, without adding decorative complexity.
- Integrate the teal verification gate into the structure more convincingly;
  it should feel load-bearing rather than a small bar placed beside the frame.
- The filled teal core can read as a stop button. Give the bounded-work element
  a more deliberate shape or relationship to the exit path while retaining a
  strong compact center.
- Preserve the strengths: one bold silhouette, two nested boundaries, one exit,
  no arrowheads, no text, no gradients, no tiny details, and excellent 48px
  survival.

Return one valid standalone SVG as structured data only. Keep the exact
`viewBox="0 0 640 420"`, transparent canvas, dark-background presentation,
Watt Mind palette, 10–18px stroke language, concise unique title/desc IDs, and
all other hard constraints from the original brief. Do not use tools or attempt
to read/write files.
